---
title: "Example: API Design & Wiring"
description: Real-world evaluation of JSON Schema typing contract vs subpath export placement.
---

# Worked Example: JSON Schema Serializer Wiring

**Status:** Completed | **Chosen public story:** Stack `A2 + B2` (Standard Schema Callback + Direct Root Export)

---

## 1. Problem Statement
Export OpenAPI / JSON Schema compliant definitions without bloating bundle sizes for consumers who only need runtime parsing.

---

## 2. Layer Map
- **Layer A (Typing & Contract):** How does the serializer function receive and infer the input schema?
- **Layer B (Placement & Export):** Where does the serializer function live in the package export map?

---

## 3. Metrics
- **Zero-Weight Tax:** Does a user importing parser pay bundle weight for JSON schema code?
- **Type Safety & Autocomplete:** Does TypeScript infer return schema types without manual generics?
- **Ecosystem Neutrality:** Does it work with standard schema libraries (Zod, ArkType, Valibot)?

---

## 4. The Hat
- `A1`: Custom Generic Wrapper Interface
- `A2`: Standard Schema Specification Contract
- `A3`: Any-Casting Escape Hatch (Rejected)
- `B1`: Method on Root Instance (`env.toJsonSchema()`)
- `B2`: Separate Subpath Export (`pkg/json-schema`)

---

## 5. Tier List & Stack Synthesis
- **🏆 S-Tier:** Stack `A2 + B2` (Standard Schema Contract + Subpath Export)
- **❄️ A-Tier (Icebox):** `B1` (Fluent Plugin Wrapper)
- **🔴 C/D-Tier:** `A3` (Type safety violation)
